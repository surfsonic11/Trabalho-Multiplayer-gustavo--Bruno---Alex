using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DragDrop : MonoBehaviour
{
   
    public bool isDraggable = true;
    public bool isMoving;
    
    
    

    //private Player m_Player

    //Se não for o GameMaker, desabiltar essa classe
    //m_Player.IsGameMaker : bool

    //DesabilitarScript()
    /*
     *{ }
     * */

    // Update is called once per frame
    //void Update()
    //{
//        if(isDragged == true)
//        {
//            if (!smartDrag)
//            {
//                transform.position = (Vector2) Camera.main.ScreenToWorldPoint(Input.mousePosition);
//}
//            else
//            {
//                transform.position = initialObjectPosition +  (Vector2) Camera.main.ScreenToWorldPoint(Input.mousePosition) - initialMousePosition;
//            }
//            if (snapToGrid)
//            {
//                transform.position = new Vector2(Mathf.RoundToInt(transform.position.x/gridSize)* gridSize, Mathf.RoundToInt(transform.position.y/gridSize)* gridSize);
//            }

    //    }
    //}

    

    

    
}
